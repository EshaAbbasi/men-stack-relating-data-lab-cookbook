<%- include('../partials/_navbar.ejs') %>

<h1>All Users</h1>

<% if (users.length === 0) { %>
  <p>No users found.</p>
<% } else { %>
  <ul>
    <% users.forEach(function(u) { %>
      <li>
        <a href="/users/<%= u._id %>"><%= u.username %></a>
      </li>
    <% }) %>
  </ul>
<% } %>